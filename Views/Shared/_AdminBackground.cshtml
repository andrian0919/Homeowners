<!-- Admin Dashboard Luxury Background -->
<div class="admin-luxury-background">
    <div class="video-overlay"></div>
    <div class="moving-gradient"></div>
</div>

<style>
    .admin-luxury-background {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background-image: url('../images/pexels-michael-tuszynski-1134777-2157401.jpg');
        background-size: cover;
        background-position: center;
        overflow: hidden;
    }
    
    .moving-gradient {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(135deg, 
                                  rgba(26, 26, 46, 0.3) 0%, 
                                  rgba(22, 33, 62, 0.3) 25%, 
                                  rgba(26, 26, 46, 0.3) 50%, 
                                  rgba(22, 33, 62, 0.3) 75%, 
                                  rgba(26, 26, 46, 0.3) 100%);
        animation: gradientMove 30s linear infinite;
        opacity: 0.7;
    }
    
    @@keyframes gradientMove {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
    
    body {
        background-color: transparent !important;
    }
    
    /* Admin-specific dashboard styling */
    .admin-dashboard-container {
        position: relative;
        z-index: 1;
    }
    
    .admin-stats-card {
        background-color: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 10px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
    }
    
    .admin-stats-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
    }
    
    .admin-dashboard-card {
        background-color: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 10px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
    }
    
    .admin-dashboard-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
    }
    
    .admin-hero-section {
        position: relative;
        border-radius: 15px;
        padding: 3rem 2rem;
        margin-bottom: 2rem;
        overflow: hidden;
        background: linear-gradient(135deg, rgba(26, 26, 46, 0.9), rgba(22, 33, 62, 0.8));
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }
</style> 